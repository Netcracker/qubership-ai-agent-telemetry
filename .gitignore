
# APM dependencies
apm_modules/

# Local agent state for targets not configured in apm.yml
.kiro/
.windsurf/
.github/instructions/
/GEMINI.md

# CLI build output
dist/
/ai-agent-telemetry
/ai-agent-telemetry.exe

# Skill eval runs (local only)
eval-workspace/

# Local backend deploy (test only, not shipped)
local-deploy-otel/

# Real .env with secrets (the .env.example template is tracked)
telemetry-backend/.env

# Ready-made telemetry config for install testing (endpoint + token; never committed)
/test-telemetry.env

# Internal SDD scratch (briefs, reports, review diffs — never shipped)
/.superpowers/
/.worktrees/
